'use strict';

describe('oopsAdmin.map module', function() {

  beforeEach(module('oopsAdmin.map'));

  describe('map controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mapCtrl = $controller('MapCtrl');
      expect(mapCtrl).toBeDefined();
    }));

  });
});
