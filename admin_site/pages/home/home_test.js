'use strict';

describe('oopsAdmin.home module', function() {

  beforeEach(module('oopsAdmin.home'));

  describe('home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('HomeCtrl');
      expect(homeCtrl).toBeDefined();
    }));

  });
});
